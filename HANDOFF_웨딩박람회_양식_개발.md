# 인수인계 — 웨딩 박람회 현장점검 양식 개발

작성일: 2026-08-14
인계 대상: Claude Code (개발 전환)
표기: **[F]** 사실 / **[O]** 판단 / **[확인필요]** 미검증 / **[TODO]** 개발 과제

---

## 0. 30초 요약

한국 예비부부가 웨딩 박람회 현장에서 업체 견적·답변을 기록하고, 구글 시트로 자동 동기화하며, 사업자등록 상태를 즉시 조회하는 **단일 파일 HTML 도구**. 빌드 시스템 없음. 모바일 브라우저 사용이 기본.

**최우선 과제(P0)**: ~~현재 저장 계층이 Claude 아티팩트 전용 API(`window.storage`)에만 의존해, 일반 브라우저에서 데이터가 저장되지 않음.~~ **해결 완료(2026-08-14).** localStorage 기본 + `window.storage` 병행 저장 어댑터 도입, 저장 실패 상단 경고 노출. 상세는 §7 P0 및 §12 참조.

---

## 1. 산출물 인벤토리

| 파일 | 역할 | 상태 |
|---|---|---|
| `박람회_현장점검_양식.html` | 메인 앱. 단일 파일, 외부 의존 0 | 저장 결함(P0) 해결 · 동작 |
| `구글시트_연동_AppsScript.gs` | 시트 upsert + 국세청 API 중계 | 미배포 |
| `상견례_협의_체크리스트.html` | 별개 도구. 동일 디자인 시스템 | 완료 |
| `결혼준비_프로젝트_현황정리_v1.md` | 도메인 컨텍스트 | 참고용 |
| `공공데이터_활용신청_활용계획서.docx/pdf` | API 신청 제출본 | 승인 완료 |

**구글 시트**: `1CAU5oDazvImVKuok6Ipfb0t4olmBH9LIeHJkILR0FDA` (탭명 현재 `Untitled`, 스크립트가 `박람회_업체` 탭 자동 생성 예정)

---

## 2. 실행 환경 제약 [F]

- **빌드 없음.** 단일 HTML 파일. 프레임워크·CDN·외부 폰트 일절 사용 금지(박람회장 통신 불안정 대비).
- **모바일 우선.** 세로 화면, 큰 탭 타깃, 입력 폰트 16px 고정(iOS 자동 확대 방지).
- **오프라인 우선.** 통신 없이 전 기능이 동작해야 하며, 동기화는 부가 기능.
- **Claude 아티팩트 미리보기에서는 외부 fetch 차단.** 동기화·API 조회는 일반 브라우저에서만 작동.
- **Apps Script 편집·배포는 모바일 불가.** 사용자는 현재 모바일만 사용 가능.

---

## 3. 아키텍처

```
[HTML 단일 파일]
  ├── 로컬 저장 (현재 window.storage — 교체 필요)
  └── fetch POST (Content-Type 미지정 → preflight 회피)
        │
        ▼
[Google Apps Script 웹앱 /exec]
  ├── action:'upsert' → 스프레드시트 행 삽입/갱신
  ├── action:'verify' → 국세청 API 중계 (CORS 회피 + 키 은닉)
  └── action:'ping'   → 연결 확인
        │
        ▼
[api.odcloud.kr/api/nts-businessman/v1/status]
```

**중계를 두는 이유**: ①브라우저에서 국세청 API 직접 호출 시 CORS 불확실 ②인증키가 클라이언트에 노출되면 안 됨.

---

## 4. 데이터 모델

### 4.1 클라이언트 레코드
```js
{
  id: 'r<base36타임스탬프><랜덤>',  // upsert 키. 절대 재생성 금지
  name, booth, type,                // 업체명, 부스, 유형
  biz, bstt, btax,                  // 사업자번호, 상태, 과세유형
  hall, meal, guar,                 // 대관료(만원), 1인식대(원), 보증인원
  memo,
  q: { [질문키]: { on: bool, a: string } },
  f: { [위험신호키]: bool },
  synced: bool                      // 시트 반영 여부
}
```
전역 상태 `D = { v: [레코드], chk: {귀가후검증}, ep: '웹앱URL' }`, 저장 키 `fair-2026-08-15`.

### 4.2 시트 스키마 (15열 고정)
`최종수정 · 레코드ID · 업체명 · 부스 · 유형 · 사업자번호 · 사업자상태 · 과세유형 · 대관료(만원) · 1인식대(원) · 보증인원 · 최소지출(만원) · 점검답변 · 위험신호 · 메모`

**Upsert 규칙**: 레코드ID 우선 → 없으면 사업자번호(숫자 10자리) → 둘 다 없으면 신규 행. 열 순서·개수를 바꾸면 `HEADERS`와 `upsert_()`의 `line` 배열을 동시에 수정할 것.

### 4.3 질문 데이터
`COMMON`(7) / `HALL`(12) / `SDM`(10) / `SNAP`(5) / `YEBOK`(4) / `HONEY`(4) — 총 42개. 형식은 `[키, 질문문장, "근거 · 위험신호"]`.

- 유형별 매핑은 `specOf(type)` 단일 함수로 통일되어 있음. **질문셋 추가 시 `specOf`만 수정하면 render·flat·export에 자동 반영됨.**
- 질문 키는 시트에 저장되지 않고 질문 문장이 저장되므로, **키 변경은 안전하나 문장 변경은 과거 기록과 불일치**를 만듦.
- 질문은 실제 소비자 분쟁·후회 사례에서 역산해 도출한 것이라 임의 축약하지 말 것. 근거는 §8 참조.

---

## 5. 동기화 로직

- 입력 → `markUnsynced(i)` → 800ms 디바운스 → `persist()`(로컬) → `push()`(시트)
- `push()`는 미동기화 + (업체명 또는 사업자번호 존재) 레코드만 전송
- 실패 시 상태표시줄에 대기 건수 표시, 데이터는 로컬 유지
- `window.addEventListener('online')` 에서 자동 재시도
- `#syncNow` 버튼은 전체 레코드 강제 재전송

---

## 6. 외부 API 사양

**엔드포인트**: `POST https://api.odcloud.kr/api/nts-businessman/v1/status?serviceKey=<Decoding키>`
**요청**: `{"b_no":["0000000000"]}`
**응답 사용 필드**: `data[0].b_stt`(계속사업자/휴업자/폐업자), `tax_type`, `end_dt`
**한도**: 1회 100건, 1일 100만건 [F]
**주의**: Decoding 키를 사용할 것. Apps Script가 `encodeURIComponent`로 인코딩하므로 Encoding 키를 넣으면 이중 인코딩되어 인증 실패.
**미사용**: 진위확인(validate) 기능. 개업일자·대표자명이 필요해 현장 사용에 부적합.
**[확인필요]** v1.1(05.31) 개정으로 응답에 `rbf_tax_type`, `rbf_tax_type_cd` 추가됨. 현재 미사용.

---

## 7. TODO — 우선순위순

### P0 — 저장 계층 교체 (블로커) — ✅ 완료 2026-08-14
- [x] `window.storage` 의존 제거. `localStorage` 기본 + `window.storage` 존재 시 병행 저장하는 어댑터 계층(`store.read`/`store.write`) 도입. 읽기는 localStorage 우선·아티팩트 폴백(기존 사용자 이전), 쓰기는 양쪽 병행
- [x] 저장 실패를 조용히 넘기지 않고 상단 경고 밴드(`#saveAlert`)에 명시 + "기록 내보내기" 버튼 제공. 저장 성공 시 자동 숨김
- [x] 대용량 대비: 용량 초과(`QuotaExceededError`) 시 전용 경고 노출. 박람회 1일 규모(부스 수십 개, 수십 KB)는 localStorage 5MB와 무관하므로 IndexedDB는 미도입 — 어댑터만 교체하면 확장 가능하도록 캡슐화

검증: Chromium 실브라우저에서 ①window.storage 부재 시 무오류 ②입력→localStorage 반영 ③새로고침 값 유지 ④용량 초과 시 경고 ⑤window.storage 병행 기록 — 5종 자동 테스트 통과.

### P1 — 현장 신뢰성
- [ ] 업체 카드 삭제 기능 없음. 오입력 시 복구 불가
- [ ] 답변 입력이 1줄 `input` — 긴 답변 시 가독성 불량. `textarea` 자동확장으로 전환 검토
- [ ] 견적서 사진 첨부(파일 → base64 → 시트 링크 또는 Drive 업로드) 미지원. **분쟁 시 증거력 확보에 중요**
- [ ] 페이지 이탈 시 미저장 경고(`beforeunload`) 없음

### P2 — 동기화 견고성
- [ ] Apps Script `fetch` 응답 판독의 브라우저별 동작 차이 [확인필요]. 실패 시 `no-cors` 폴백 + 낙관적 성공 처리 검토
- [ ] 재시도에 지수 백오프 없음
- [ ] 시트 측 동시 편집 충돌 처리 없음(단일 사용자 전제)
- [ ] Apps Script 웹앱 URL이 평문 저장됨. 유출 시 누구나 시트에 쓰기 가능 → 공유 시크릿 토큰 추가 검토

### P3 — 기능 확장
- [ ] 업체 간 견적 비교 뷰(동일 보증인원 기준 정규화)
- [ ] 위험신호 개수 기준 자동 순위 하향 표시
- [ ] 예식장 유형에서 보증 120/150/200 총액 3종 동시 입력·비교
- [ ] 상견례 체크리스트와 데이터 연동(견적 → 예산표 자동 반영)

---

## 8. 도메인 컨텍스트 (질문 설계 근거)

**사용자 조건**: 예식 2027 하반기~2028 상반기 / 부산 유력 / 하객 135~165명(신랑혼주 80~110·신부혼주 30·본인 25) / 예식 준비비 3,000만원 이하 / 스튜디오 촬영 수도권·드메 부산 분리 / 예물 커플링만 / 축의금 정산 하한 A안(식대정산)

**왜 이 질문들인가** [F]
- 결혼서비스 피해구제 2024년 905건 → 2025년 1,076건. 분쟁의 88.1%가 계약해지·위약금·청약철회 (공정위·한국소비자원)
- 웨딩박람회 피해 유형: 청약철회 거부 46.8%, 과다위약금 43.0%, 계약불이행 8.1%
- 각보증(신랑·신부 각각 보증)은 하객 3:1 비대칭 구조인 이 사용자에게 치명적 → `h1`에 최상단 배치
- 소비자분쟁해결기준상 예식 150일 전 전액환급이 원칙이나 "환불 불가" 특약이 흔함 → `h11`
- 스드메 3대 필수 추가금(원본비·헬퍼비·피팅비)이 기본가에서 누락되는 구조 → `s1`

**질문 순서는 금액 영향 크기순.** 재배열 시 이 원칙을 유지할 것.

---

## 9. 검증 절차

```
1. 구문   : node -e "new Function(html.match(/<script>([\s\S]*)<\/script>/)[1])"
2. 저장   : 입력 → 새로고침 → 값 유지 확인 (일반 브라우저에서)
3. 연결   : 연동 설정 → 연결 테스트 → "시트: 박람회_업체 · API 키 등록됨"
4. Upsert : 동일 레코드 2회 저장 → 시트 행이 1개인지 확인
5. 조회   : 유효 사업자번호 → "계속사업자 · 부가가치세 일반과세자"
6. 오프라인: 비행기모드 → 입력 → 온라인 복귀 → 자동 재전송 확인
```

---

## 10. 개발 시 지켜야 할 것

- **단일 파일 유지.** 분할·번들러 도입 금지
- **외부 리소스 참조 금지.** 폰트·아이콘·라이브러리 전부
- **디자인 토큰 유지**: `--ink #12161c` / `--paper #eeeae2` / `--seal #8c2f26`(경고) / `--amber #a86a12`(위험신호) / `--ok #2c5c46`(정상). 상단 고정 경고 밴드는 이 도구의 유일한 강조 요소이므로 제거·희석하지 말 것
- **법령·금액 수치를 임의로 갱신하지 말 것.** 위약금 구간(90일 vs 150일 상충)은 미확정 상태이며 원문 대조 전까지 현행 문구 유지
- 어조: 건조·개조식. 사용자 지침상 아부·빈말 금지

---

## 11. 미해결 [확인필요]

1. 소비자분쟁해결기준 전액환급 기준 90일 vs 150일 상충 — law.go.kr 원문
2. 2025.12.18 개정 위약금 구간(29~10일 40% / 9~1일 50% / 당일 70%)의 149~60일·59~30일 구간 유지 여부
3. Apps Script 웹앱 POST 응답 판독의 브라우저별 동작 차이
4. 국세청 API v1.1 응답 필드 변경이 현재 파싱에 미치는 영향
5. 신규 개업 사업자의 국세청 반영 지연(1~2일)이 조회 결과에 미치는 영향

---

## 12. 개발 로그

### 2026-08-14 — P0 저장 계층 교체
- **문제**: 저장이 Claude 아티팩트 전용 `window.storage`에만 의존 → 일반 브라우저에서 새로고침 시 데이터 유실(블로커).
- **변경(`박람회_현장점검_양식.html`, 스크립트 상단 어댑터)**:
  - `store` 어댑터 도입. `read()`=localStorage 우선·`window.storage` 폴백, `write()`=양쪽 병행 기록(한 곳이라도 성공하면 유실 아님).
  - `load()`/`persist()`가 `window.storage`를 직접 호출하지 않고 어댑터만 사용. 남은 `window.storage` 참조는 어댑터 내부 존재확인·병행쓰기뿐.
  - 저장 실패 시 상단 경고 밴드 `#saveAlert`(seal 색) + "기록 내보내기" 버튼 노출, 성공 시 자동 숨김. 상태표시줄 문구도 유지.
  - 용량 초과(`QuotaExceededError`/code 22·1014) 전용 메시지. 읽기 차단(시크릿 모드) 시 로드 시점에 경고.
- **미도입 결정**: IndexedDB. 현 데이터 규모(수십 KB)가 5MB 한계와 무관. 어댑터 경계만 교체하면 후속 도입 가능하도록 캡슐화해 둠.
- **디자인 원칙 준수**: 단일 파일·외부 의존 0 유지. 상단 고정 경고 밴드(계약 금지 원칙)는 그대로 두고, 저장 경고는 색·위치가 구분되는 별도 요소로 추가.
- **검증**: Chromium 자동 테스트 5종 통과(§7 P0 참조).

---
*작성일 2026-08-14 · 근거: 한국소비자원 참가격(2025.4·2026.8), 공정거래위원회 표준약관·피해예방주의보(2026.4.22), 공공데이터포털 국세청 사업자등록정보 조회 서비스 상세, 사용자 확정 입력*
*미확인 항목: §11 전체 5건*
