# 배포 가이드 — PC에서 1회 (복붙 위주, 약 10분)

박람회 현장점검 온라인 웹앱(입력 폼 + 분석·보고서 + GitHub 정합성 원장 + 구글 시트)을 살리는 절차.
**모바일 불가 · PC 필요.** 아래 순서대로 복사·붙여넣기만 하면 됩니다.

리포지토리 파일 위치(브랜치 `claude/handoff-p0-tasks-5ep8hj`):
- 백엔드: `구글시트_연동_AppsScript.gs`
- 입력 폼: `박람회_현장점검_양식.html`
- 보고서: `분석보고서.html`
- (자동 생성) 원장 데이터: `data/fair-data.json`

GitHub에서 각 파일을 열고 우측 **Raw** 또는 **⧉(Copy raw file)** 버튼으로 전체 복사하면 편합니다.

---

## 0. 준비물 — 인증키 2개
둘 다 **코드에 넣지 않고** Apps Script 스크립트 속성에만 저장합니다.

1. **GITHUB_TOKEN** (필수 — GitHub 원장 커밋용)
   - GitHub → Settings → Developer settings → Personal access tokens → **Fine-grained tokens** → Generate
   - Resource owner: `Alfira0526` / Repository access: **Only select repositories → Sample**
   - Permissions → Repository → **Contents = Read and write** (Metadata read-only 자동). 그 외 없음
   - Generate → 토큰 문자열 복사(한 번만 보임)

2. **NTS_API_KEY** (선택 — 사업자 조회용, 없으면 조회만 비활성)
   - data.go.kr → "국세청_사업자등록정보 진위확인 및 상태조회" 활용신청 → **일반 인증키(Decoding)** 복사
   - ⚠ **이전에 코드에 있던 키는 공개 저장소에 노출됐으므로, data.go.kr 마이페이지에서 재발급 후 새 키를 쓰세요.**

---

## 1. 스프레드시트 + Apps Script 프로젝트
1. 구글 스프레드시트 새로 만들기
2. **확장 프로그램 › Apps Script**

## 2. 코드 붙여넣기 — 파일 하나면 됨
Apps Script 편집기에서:
1. 기본 파일 **`Code.gs`** 내용 전체 삭제 → `구글시트_연동_AppsScript.gs` 전체 붙여넣기
   > **화면(HTML)은 GitHub에서 자동으로 가져와 서빙**하므로 form/report HTML 파일은 만들 필요 없습니다.
   > 이후 HTML을 고쳐도 **재붙여넣기·재배포 불필요**(GitHub에 올리면 최대 30초 내 자동 반영, 즉시는 URL 끝에 `?refresh=1`).
   > `Code.gs`(백엔드)를 바꿀 때만 다시 붙여넣고 "새 버전"으로 재배포하면 됩니다.

## 3. 스크립트 속성에 인증키 저장
1. 좌측 **⚙ 프로젝트 설정(Project Settings)**
2. 아래 **스크립트 속성(Script properties)** → **스크립트 속성 추가**
   - `GITHUB_TOKEN` = 위에서 만든 파인그레인드 토큰
   - `NTS_API_KEY` = 공공데이터포털 Decoding 키 (선택)
3. 저장

## 4. 배포
1. 우상단 **배포 › 새 배포**
2. 유형(⚙) → **웹 앱**
3. 실행 계정: **나**, 액세스 권한: **모든 사용자**
4. **배포** → 권한 승인 → 웹 앱 URL(`…/exec`) 복사
   > 코드 수정 후에는 **배포 › 배포 관리 › (연필) › 버전 "새 버전" › 배포** 로 재배포해야 반영됨

접속 링크:
- `…/exec` → 입력 폼
- `…/exec?page=report` → 분석·보고서 대시보드(인쇄 → PDF 보고서)

---

## 5. 확인 (2분)
1. `…/exec` 열기 → 상단 연동 설정 배지가 **"내장 서버"** 로 표시되는지
2. 업체 1개 입력(예식장 · 대관료·1인식대·보증인원) → 잠시 후 상태표시줄 **"저장 완료 · GitHub 원장 반영"**
3. 구글 시트에 `박람회_업체` 탭 생성 + 행 추가 확인
4. 리포지토리 `data/fair-data.json` 에 새 커밋(검증완료) 생겼는지 확인
5. `…/exec?page=report` → KPI·예식장 최소지출 비교 표시 → **보고서 인쇄·PDF**
6. 상견례 예산표(`상견례_협의_체크리스트.html`)를 열면 예식장 행·총지출에 실측 견적 자동 반영(GitHub 온라인)

---

## 6. 브랜치 주의
현재 코드가 작업 브랜치 `claude/handoff-p0-tasks-5ep8hj` 를 가리킵니다.
나중에 기본 브랜치(main 등)로 병합하면 아래 3곳의 브랜치명을 그 브랜치로 바꾸세요:
- `구글시트_연동_AppsScript.gs` → `GITHUB_BRANCH`
- `분석보고서.html` (해당 없음 — 시트에서 읽음)
- `상견례_협의_체크리스트.html` → `FEED_URL`

## 7. 문제 해결
- **저장은 되는데 GitHub 커밋 안 됨**: `GITHUB_TOKEN` 미설정/권한 부족(Contents=Read/Write 확인) 또는 `GITHUB_BRANCH` 불일치. 토큰 없으면 시트 저장만 되고 원장은 건너뜀(정상 동작).
- **사업자 조회 실패**: `NTS_API_KEY` 미설정 또는 Encoding 키를 넣음(반드시 **Decoding** 키). 승인까지 시간이 걸릴 수 있음.
- **폼/보고서 안 열림**: HTML 파일명이 `form`/`report` 인지, 배포를 "새 버전"으로 했는지 확인.
- **예산표에 반영 안 됨**: 저장소가 공개인지, `FEED_URL` 브랜치가 맞는지 확인. 오프라인이면 같은 브라우저 로컬값으로 폴백.
